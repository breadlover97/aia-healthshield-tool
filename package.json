{
  "name": "aia-healthshield-tool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:premiums": "node scripts/build-premium-table.mjs",
    "serve": "node scripts/serve.mjs",
    "validate": "npm run build:premiums && node scripts/validate-claims.mjs"
  }
}
